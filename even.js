//is this the same as isEven(num)? what is the advantage of declaring an annonymous function as a variable?
const isEven = function(num) {
  return num % 2 === 0;
}

const tenIsEven = isEven(10);
console.log(tenIsEven);

//or, in a simplified way:

consele.log(isEven(11));